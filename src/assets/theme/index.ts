import { createTheme } from "@mui/material";

const MuiTheme: any = createTheme({
  palette: {
    primary: {
      main: "#038112",
      light: "#fff",
    },
    secondary: {
      main: "#000",
    },
  },
});

export default MuiTheme;
